---
import NewPostPage from '../../components/NewPostPage.astro';
import { isContentType } from '../../lib/content-types';

const { type } = Astro.params;
if (!isContentType(type)) return Astro.redirect('/');
---

<NewPostPage type={type} />
